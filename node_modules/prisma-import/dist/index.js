#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const internals_1 = require("@prisma/internals");
const chalk_1 = __importDefault(require("chalk"));
const prompts_1 = __importDefault(require("prompts"));
const path_1 = __importDefault(require("path"));
const util_1 = require("./util");
const merge_1 = require("./merge");
const main = () => __awaiter(void 0, void 0, void 0, function* () {
    const args = (0, internals_1.arg)(process.argv.slice(2), {
        '--schemas': [String],
        '-s': '--schemas',
        '--output': String,
        '-o': '--output',
        '--dry': Boolean,
        '-d': '--dry',
        '--help': Boolean,
        '-h': '--help',
        '--force': Boolean,
        '-f': '--force',
    });
    if (args instanceof Error) {
        throw args.message;
    }
    if (args['--help']) {
        console.log((0, internals_1.format)(`
Compile all your schemas into one and replace all imports

${chalk_1.default.bold('Usage')}

  ${chalk_1.default.dim('$')} prisma-import [flags/options]

${chalk_1.default.bold('Flags')}

          -h, --help    Display this help message
          -d, --dry     Print the resulting schema to stdout
          -f, --force   Skip asking for confirmation on schema overwrite
          -o, --output  Specify where you want your resulting schema to be written
          -s, --schemas Specify where your schemas are using a glob pattern
    `));
        process.exit(0);
    }
    //
    // Get glob
    //
    let schemasGlob = args['--schemas'];
    if (!args['--schemas']) {
        const config = yield (0, util_1.getConfigFromPackageJson)('schemas');
        schemasGlob = config ? [config] : undefined;
    }
    if (!(schemasGlob === null || schemasGlob === void 0 ? void 0 : schemasGlob.length)) {
        throw new Error('Provide a glob pattern to find your schemas. Either pass it with `--schemas` or add it to your package.json at `prisma.import.schemas`');
    }
    const schemaPaths = (yield Promise.all(schemasGlob.map((s) => __awaiter(void 0, void 0, void 0, function* () {
        return yield (0, util_1.glob)(s, {
            cwd: process.cwd(),
        });
    })))).flat();
    if (!schemaPaths.length) {
        throw new Error(`No schemas found using glob pattern \`${schemasGlob.join(', ')}\``);
    }
    //
    // Get output
    //
    let relativeOutputPath = args['--output'];
    if (!args['--output']) {
        relativeOutputPath = yield (0, util_1.getConfigFromPackageJson)('output');
    }
    if (!relativeOutputPath) {
        throw new Error('Provide an output path. Either pass it with `--output` or add it to your package.json at `prisma.import.output`');
    }
    const absoluteOutputPath = path_1.default.isAbsolute(relativeOutputPath) ? relativeOutputPath : path_1.default.resolve(relativeOutputPath);
    //
    // Confirm schema overwrite
    //
    const dryMode = Boolean(args['--dry']);
    const forceMode = Boolean(args['--force']);
    if (dryMode) {
        console.log(`✔ Running in ${chalk_1.default.blueBright('dry mode')}\n`);
    }
    if (!forceMode && !dryMode && (yield (0, util_1.exists)(absoluteOutputPath))) {
        const result = yield (0, prompts_1.default)({
            type: 'confirm',
            name: 'confirm',
            message: `This will ${chalk_1.default.red('overwrite')} your output schema. You can disable this prompt with the ${chalk_1.default.blueBright('--force')} option. Continue?`,
        });
        if (!result.confirm) {
            process.exit(1);
        }
    }
    yield (0, merge_1.merge)(schemaPaths, absoluteOutputPath, dryMode);
});
void main();
