"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.merge = void 0;
const path_1 = __importDefault(require("path"));
const util_1 = require("./util");
const prisma_fmt_wasm_1 = require("@prisma/prisma-fmt-wasm");
const url_1 = require("url");
const merge = (schemaPaths, outputPath, dry) => __awaiter(void 0, void 0, void 0, function* () {
    const headerContent = [
        '//',
        `// Autogenerated by \`prisma-import\``,
        '// Any modifications will be overwritten on subsequent runs.',
        '//',
        '',
    ];
    const outputContent = [];
    const datasoureContent = [];
    for (const schemaPath of schemaPaths) {
        let content = yield (0, util_1.readFile)(schemaPath, 'utf-8');
        content = content
            .split('\n')
            .filter((line) => !/^(?<=\s*)(import\s*{.*)/g.test(line))
            .join('\n');
        const pushedContent = [];
        pushedContent.push('\n');
        pushedContent.push('//');
        pushedContent.push(`// ${path_1.default.basename(schemaPath)}`);
        pushedContent.push('//');
        pushedContent.push('\n');
        pushedContent.push(content);
        if (/^\s*generator.*/g.test(content) || /^\s*datasource.*/g.test(content)) {
            datasoureContent.push(...pushedContent);
        }
        else {
            outputContent.push(...pushedContent);
        }
    }
    const resultingSchema = (0, prisma_fmt_wasm_1.format)([...headerContent, ...datasoureContent, ...outputContent].join('\n'), JSON.stringify({
        textDocument: {
            uri: (0, url_1.pathToFileURL)(outputPath),
        },
        options: {
            tabSize: 2,
            insertSpaces: true,
        },
    }));
    if (dry) {
        console.log(resultingSchema);
    }
    else {
        yield (0, util_1.ensureDirectoryExistence)(outputPath);
        yield (0, util_1.writeFile)(outputPath, resultingSchema);
    }
});
exports.merge = merge;
